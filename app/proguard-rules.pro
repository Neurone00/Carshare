# Components are referenced from the manifest and kept automatically.
# Keep the core module's public API readable in stack traces.
-keepattributes SourceFile,LineNumberTable
-dontwarn org.jetbrains.annotations.**
